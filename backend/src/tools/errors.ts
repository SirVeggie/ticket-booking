
const noData = 'Data not found';
const invalidData = 'Object contains invalid data';
const illegalData = 'Data was recognized, but was discarded';
const noAdmin = 'Invalid admin token';
const invalidIP = 'Token ip did not match';
const tokenExpire = 'Token has expired';

export default { noData, invalidData, illegalData, noAdmin, invalidIP, tokenExpire };